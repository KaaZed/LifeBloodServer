diff --git a/diag.py b/diag.py
index 19099b4..d2996fb 100644
--- a/diag.py
+++ b/diag.py
@@ -1,9 +1,9 @@
 import os, asyncio, subprocess, httpx
 from dotenv import load_dotenv

 load_dotenv()

+async def main():
     print("TOKEN:", os.getenv("TELEGRAM_BOT_TOKEN"))
     print("DB_DSN:", os.getenv("DB_DSN"))
@@ -15,11 +15,12 @@ async def main():
         print("getMe error:", e)
     print(subprocess.check_output(['ss', '-tulpen']).decode())

+if __name__ == "__main__":
     asyncio.run(main())
-    import psutil
-for c in psutil.net_connections(kind="inet"):
-    if c.status == psutil.CONN_LISTEN:
-        proc = psutil.Process(c.pid).name() if c.pid else "?"
-        print(f"{c.laddr.ip}:{c.laddr.port} -> PID {c.pid} {proc}")
+    import psutil
+    for c in psutil.net_connections(kind="inet"):
+        if c.status == psutil.CONN_LISTEN:
+            proc = psutil.Process(c.pid).name() if c.pid else "?"
+            print(f"{c.laddr.ip}:{c.laddr.port} -> PID {c.pid} {proc}")
+

diff --git a/logger.py b/logger.py
index 29be5e8..6e945bd 100644
--- a/logger.py
+++ b/logger.py
@@ -38,4 +38,4 @@ def err(msg: str) -> bool:
         sys.stderr.write(line + "\n")
     except Exception:
         pass
-    return success
+    return success

diff --git a/patch_dashboard.py b/patch_dashboard.py
index 664f05c..8b02c6e 100644
--- a/patch_dashboard.py
+++ b/patch_dashboard.py
@@ -20,3 +20,4 @@ src = src.replace(
 path.write_text(src, encoding="utf-8")
 print("patch applied")
+

diff --git a/settings.py b/settings.py
index abd94e9..c2f87a7 100644
--- a/settings.py
+++ b/settings.py
@@ -48,4 +48,5 @@ if not ADMIN_API_TOKEN:
 #       cluster = int(app_cfg.get("CLUSTER_RADIUS_METERS","100"))
 async def load_app_settings(conn) -> Dict[str,str]:
     rows = await conn.fetch("SELECT key, value FROM app_settings")
-    return {r["key"]: r["value"] for r in rows}
+    return {r["key"]: r["value"] for r in rows}
+

diff --git a/tg_api.py b/tg_api.py
index 5b6c276..5ae3e2f 100644
--- a/tg_api.py
+++ b/tg_api.py
@@ -53,3 +53,4 @@ async def delete_message(chat_id: int, message_id: int) -> Tuple[bool, Dict[str,
     payload = {"chat_id": chat_id, "message_id": message_id}
     info(f"[tg] delete_message → chat:{chat_id}, msg:{message_id}")
     return await _post("deleteMessage", payload)
+

diff --git a/webhook_app.py b/webhook_app.py
index 41177e8..842a6c4 100644
--- a/webhook_app.py
+++ b/webhook_app.py
@@ -58,16 +57,31 @@ def _fmt_lbc(x: Any) -> str:
     except Exception:
         return "0.00000"

-def _sig(stats: Dict[str, Any]) -> Tuple[int,int,float,float,int,int,str]:
+def _fi(x: Any) -> int:
+    try:
+        return int(x or 0)
+    except Exception:
+        return 0
+
+def _ff(x: Any) -> float:
+    try:
+        return float(x or 0.0)
+    except Exception:
+        return 0.0
+
+def _sig(stats: Dict[str, Any]) -> Tuple[int, int, float, float, int, int, str]:
     s = stats or {}
-    return (
-        _fi(s.get('today_steps')),
-        _fi(s.get('total_steps')),
-        _ff(s.get('today_lbc')),
-        _ff(s.get('total_lbc')),
-        _fi(s.get('energy_left')),
-        _fi(s.get('energy_max')),
-        (s.get('reason_if_not_counted') or "").strip(),
-    )
+    return (
+        _fi(s.get("today_steps")),
+        _fi(s.get("total_steps")),
+        _ff(s.get("today_lbc")),
+        _ff(s.get("total_lbc")),
+        _fi(s.get("energy_left")),
+        _fi(s.get("energy_max")),
+        (s.get("reason_if_not_counted") or "").strip(),
+    )

 def _dash_text(stats: Dict[str, Any]) -> str:
     s = stats or {}
@@ -237,3 +251,4 @@ async def webhook(request: Request):
         return JSONResponse({"ok": True})

     return JSONResponse({"ok": True})
+
